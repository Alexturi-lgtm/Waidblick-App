# fastlane Appfile (iOS) — Gamssinn
#
# Bundle-ID ist UNVERAENDERLICH: Apple-Profil ist genau dafuer ausgestellt.
app_identifier("de.waidblick.app")

# Apple Team-ID (aus ios/ExportOptions.plist).
team_id("J4JZ2DQC77")

# Hinweis: Login erfolgt NICHT ueber apple_id/Passwort, sondern ueber den
# App Store Connect API Key (siehe Fastfile). Daher hier bewusst kein apple_id.
